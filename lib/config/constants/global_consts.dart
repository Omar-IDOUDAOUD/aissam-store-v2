abstract class GlobalConstnts {
  static const Duration requestTimeoutDuration = Duration(seconds: 20);
  static const Duration cacheExpirationPeriod = Duration(days: 10);
  static const String userLocalDataDir = 'user_data';
  static const String userRemoteCollection = 'users'; 
}
